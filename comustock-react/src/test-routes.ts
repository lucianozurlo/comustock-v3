/**
 * Route Testing Utility
 *
 * This file can be used to verify that all routes are properly configured.
 * Import and log the router configuration to inspect the route structure.
 */
import router from './routes';

// Log all routes for verification
console.log('Router configuration:', router);
console.log('Routes:', router.routes);

export default router;
